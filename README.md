**Create Env**
`python -m venv .venv`

**Run Env (Windows)**
`.venv\Scripts\activate`

**Dependencies**
`pip install Flask`
`pip install python-dotenv`
`pip install watchdog`

**Run Development Server (app.py)**
`flask run --debug`
